name: Multithreading-iOS-Case
options:
  bundleIdPrefix: com.dsgnmike.multithreading
targets:
  MultithreadingiOSCase:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - path: Sources
      - path: Resources/Assets.xcassets
      - path: Base/LaunchScreen.storyboard
    dependencies:
      - sdk: UIKit.framework
    info:
      path: Resources/Info.plist
      properties:
        CFBundleVersion: "1.0"
        CFBundleShortVersionString: "1.0"
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
          UISceneConfigurations:
            UIWindowSceneSessionRoleApplication:
              - UISceneConfigurationName: "Default Configuration"
                UISceneDelegateClassName: "$(PRODUCT_MODULE_NAME).SceneDelegate"
    settings:
      TARGETED_DEVICE_FAMILY: 1
      SWIFT_VERSION: 5.0
      UILaunchScreenName: LaunchScreen

  MultithreadingiOSCaseTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - path: Tests
    dependencies:
      - target: MultithreadingiOSCase
      - sdk: XCTest.framework
    info:
      path: Resources/Info.plist
      properties:
        CFBundleVersion: "1.0"
        CFBundleShortVersionString: "1.0"
    settings:
      SWIFT_VERSION: 5.0